//========== Info ==========
name = SquareTest
category = scene2d


//========== Settings ==========

// The setup will be placed in its own folder and can be a gradle project,
// have a readme file and its own lauchers.
// If create folder is false the setup can not be a gradle project and have a readme.
// It will instead share the category's readme and gradle project.

createFolder = true
createReadme = true
isGradleProject = true

